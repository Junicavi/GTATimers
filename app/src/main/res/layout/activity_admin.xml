<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/backLayout"
    tools:context=".Admin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:layout_marginLeft="50dp"
        android:layout_marginTop="50dp"
        android:layout_marginRight="50dp"
        android:layout_marginBottom="50dp"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/logo"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="0.3"
            android:layout_gravity="center"
            android:contentDescription="@string/main_logo"
            android:src="@drawable/logo" />


        <Button
            android:layout_gravity="center"
            android:id="@+id/cargo_warehouse_button"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.3"
            android:onClick="mainActivity"
            android:background="@drawable/admin_btn_bg1"
            android:text="GTA TIMERS"
            android:textColor="@color/colorPrimary"
            android:textStyle="bold" />
            <!--android:layout_marginBottom="25dp"-->

        <Button
            android:layout_gravity="center"
            android:id="@+id/car_warehouse_button"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="0.3"
            android:onClick="settingsActivity"
            android:background="@drawable/admin_btn_bg2"
            android:text="ADMINISTRACIÓN GTA TIMERS"
            android:textColor="@color/colorPrimary"
            android:textStyle="bold" />
            <!--android:layout_marginBottom="20dp"-->

    </LinearLayout>
</RelativeLayout>